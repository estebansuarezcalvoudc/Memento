import SectionHeader from './SectionHeader'
import { Section } from './Section'

export default function MeetingProcessingSettings() {
  return <SectionHeader section={Section.MeetingsProcessing} />
}
